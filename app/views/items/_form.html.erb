<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <font color="#ffffff"><%= f.label :Bericht %><br /></font>
    <%= f.text_area(:what, :size => "60x10") %>
  </div>
  <div class="field">
    <font color="#ffffff"><%= f.label :Stichpunkte %><br /></font>
    <%= f.text_area(:how_much, :size => "20x10") %>
  </div>
  <div>
    <font color="#ffffff"><%= f.label :datum %><br /></font>
    <%= f.text_field :datum %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
